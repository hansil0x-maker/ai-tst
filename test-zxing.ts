import { MultiFormatReader, BarcodeFormat, DecodeHintType, RGBLuminanceSource, BinaryBitmap, HybridBinarizer } from '@zxing/library';
import { Jimp } from 'jimp';

async function run() {
  // Create a simple barcode with JsBarcode and read it
  // Actually let's just write a script to see if it imports
  console.log("imported");
}
run();
